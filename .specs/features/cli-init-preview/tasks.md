# Tasks

1. Align the installed `init` manifest and parser contract with the Slice A public interface; verify contract tests; commit atomically.
2. Compose the dry-run command through `SafeInitService.preview()` and render structured results safely; verify focused CLI tests; commit atomically.
3. Add launcher E2E from a disposable Git fixture proving deterministic, read-only preview; run `gate:quick`; commit atomically.
4. Run build/security/release gates and independent validation before PR.
