# Tasks

1. Inventory every trust/persistent canonicalizer and its byte consumers.
2. Define the inward canonical JSON port and compatibility/version matrix.
3. Migrate one compatibility-safe vertical slice with cross-locale and
   discrimination tests.
4. Migrate remaining slices only after their persisted-byte rules are reviewed.
