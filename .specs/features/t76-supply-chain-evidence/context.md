# Context

T76's candidate contract reserves license, SBOM, provenance, and evaluation
components, but no shared production boundary generated the bytes that those
component identities describe. This slice supplies a deterministic generator
and verifier from already collected hermetic component metadata.

The documents are unsigned inputs. DSSE signing, TUF publication, offline
views, rollback execution, and the independent T76 report remain separate
release tasks.
